elasticsearch:
  port: -1
  enabled: false
  indexname: testindex
spring:
  application:
    name: Contented
  sleuth:
    reactor:
      instrumentation-type: manual
  data:
    mongodb:
      #      Crucial for flapdoodle in-memory mongodb to work
      port: 0
#    elasticsearch:
#      client:
#        reactive:
#          endpoints: "localhost:${elasticsearch.port}"
